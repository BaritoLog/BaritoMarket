.row.justify-content-center
  .col-md-6
    h3 Edit Helm Cluster Template
    - if flash.key?(:messages)
      .alert.alert-danger
        b Issue editing helm cluster template
        br
        - flash[:messages].each do |msg|
          = "- #{msg}"
          br
    #edit_helm_cluster_template
      = render 'form', helm_cluster_template: @helm_cluster_template
